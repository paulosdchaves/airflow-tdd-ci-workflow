CREATE TABLE IF NOT EXISTS {tablename} (
    quotation_id             INTEGER,
    createdAt                DATE,
    user_id                  INTEGER,
    professionalActivity_id  INTEGER
    broker_id                INTEGER
);